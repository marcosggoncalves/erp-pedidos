program erp_pedido;

uses
  Vcl.Forms,
  uCliente in 'src\modulos\clientes\uCliente.pas' {Cliente};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TCliente, Cliente);
  Application.Run;
end.
